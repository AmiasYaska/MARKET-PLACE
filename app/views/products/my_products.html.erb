<div class="w-full">   
    <% if @products.any? %>
        <% if notice.present? %>
            <p class="py-2 px-3 bg-green-50 mb-5 text-green-500 font-medium rounded-md inline-block" id="notice"><%= notice %></p>
        <% end %>

        <div id="products" class="grid grid-cols-2 md:grid-cols-4 gap-5">
            <% if @products.any? %>
            <% @products.each do |product| %>
                <div class="border rounded-lg">
                <div class="m-3">
                    <%= link_to product.title, product, class:"text-xl " %>
                </div>
                <% if product.images.any? %>
                    <%= link_to product do %>
                    <%= image_tag product.images.first, class:"w-full h-48 rounded-md" %>
                    <% end %>
                <% end %>
                <div class="m-3 items-center">
                    <%= link_to product.price, product, class:"text-lg" %>
                </div>
                </div>
            <% end %>
            <% else %>
            <p class="text-center my-10">No products found.</p>
            <% end %>
        </div>

        <div class="mt-10">
            <%= link_to 'Add a product', new_product_path, class:"py-2 px-3 rounded-lg text-white bg-blue-500" %>
        </div>

    <% else %>
        <p>You have no products so far.</p>
        <div class="mt-10">
            <%= link_to 'Add a product', new_product_path, class:"py-2 px-3 rounded-lg text-white bg-blue-500" %>
        </div>
    <% end %>
    
</div>